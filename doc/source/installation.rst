============
Installation
============

At the command line::

    $ pip install mesocyclone

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv mesocyclone
    $ pip install mesocyclone
